# Ажлын лог - 2025-12-19

## QC Control Charts - Dry Basis шилжилт

### Хийсэн ажлууд ✅

#### 1. Backend - Dry Basis тооцоолол (control_charts.py)

**Файл**: `app/routes/quality/control_charts.py`

```python
# Нэмсэн mapping-үүд:
DRY_BASIS_MAPPING = {
    'Ad': ('ash_dry', 'Aad'),      # Үнс хуурай
    'Vd': ('volatiles_dry', 'Vad'), # Дэгдэмхий хуурай
    'CV,d': ('cv_dry', 'CV'),       # Илчлэг хуурай
    'St,d': ('ts_dry', 'TS'),       # Хүхэр хуурай
}

ALIAS_MAPPING = {
    'TRD,d': 'TRD',  # TRD аль хэдийн хуурай төлөвөөр хэмжигддэг
}
```

**Тооцооллын томьёо**:
```
dry_value = air_dry_value * 100 / (100 - Mad)
```

**Статус**: ✅ Дууссан

---

#### 2. Backend - AnalysisResultLog ашиглах

**Өмнө**: `AnalysisResult` хүснэгтээс (зөвхөн сүүлийн үр дүн)
**Одоо**: `AnalysisResultLog` хүснэгтээс (бүх түүхэн үр дүн)

Энэ өөрчлөлтөөр нэг дээж дээр олон удаа хэмжсэн үр дүнгүүд бүгд QC chart дээр харагдана.

**Статус**: ✅ Дууссан

---

#### 3. CM таргет утгууд

| Шинжилгээ | Target | SD |
|-----------|--------|-----|
| Mad | 0.64 | 1 |
| Ad | 10.89 | 0.04 |
| Vd | 24.22 | 0.22 |
| CV,d | 7696 | 52 |
| St,d | 0.71 | 0.01 |
| TRD,d | 1.38 | 0.01 |

**Статус**: ✅ Баталгаажсан

---

#### 4. Frontend filter засах (control_charts.html)

**Файл**: `app/templates/quality/control_charts.html`

**Асуудал**: JavaScript код `sample_code`-оор хэт хатуу filter хийж байсан:
- `CM-2025-Q4` формат ≠ `CM_20251128_N_Q4` (хуучин формат)
- Үүнээс болж олон цэг харагдахгүй байсан

**Засвар**:

1. **loadChartData функц** (lines 474-482):
```javascript
// ХУУЧИН - устгасан:
const pts = data.data_points
  .filter(p => (p.sample_code || '').toUpperCase().startsWith(sn.toUpperCase()))
  .sort(...);

// ШИНЭ:
const pts = data.data_points.sort((a, b) => new Date(a.date) - new Date(b.date));
```

2. **loadModalChart функц** (lines 609-611):
```javascript
// ХУУЧИН - устгасан:
const pts = data.data_points
  .filter(p => (p.sample_code || '').toUpperCase().startsWith(standardName.toUpperCase()))
  .sort(...);

// ШИНЭ:
const pts = data.data_points.sort((a, b) => new Date(b.date) - new Date(a.date));
```

**Статус**: ✅ Дууссан

---

### Дүгнэлт

| # | Ажил | Статус |
|---|------|--------|
| 1 | DRY_BASIS_MAPPING нэмсэн | ✅ |
| 2 | ALIAS_MAPPING нэмсэн (TRD,d → TRD) | ✅ |
| 3 | AnalysisResultLog-оос түүх унших | ✅ |
| 4 | Frontend filter хасах | ✅ |

**Бүх ажил амжилттай дууссан!**

---

### Тест

Серверийг ажиллуулаад `/quality/control-charts` хуудсанд орж шалгана уу.

```bash
cd D:/coal_lims_project
venv/Scripts/python.exe -m flask run --host=0.0.0.0
```

Хүлээгдэж буй үр дүн:
- CV,d chart дээр 18 цэг харагдах ёстой
- Бүх хуучин формат дээжүүд (CM_20251128_N_Q4 гэх мэт) харагдах ёстой

---

### Холбогдох файлууд

1. `app/routes/quality/control_charts.py` - Backend API
2. `app/templates/quality/control_charts.html` - Frontend template
3. `app/models.py` - AnalysisResultLog model

---

*Лог бичсэн: Claude Code*
*Огноо: 2025-12-19*
*Статус: Бүгд дууссан ✅*
