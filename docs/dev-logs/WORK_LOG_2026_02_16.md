# 2026-02-16 Хийсэн ажлууд

## Excel Export System - ДУУССАН ✅

### SheetJS (xlsx) нэмсэн
- `app/static/js/xlsx.full.min.js` — local файл (CSP хязгаарлалт)
- Глобал helper функцүүд `base.html`-д:
  - `gridToXlsx(api, fileName, sheetName)` — AG-Grid → .xlsx
  - `tableToXlsx(tableId, fileName, sheetName)` — HTML table → .xlsx
- `gridToXlsx` нь `valueGetter`/`valueFormatter` дэмждэг (аудит хуудасны p1/p2 баганууд)

### Шинэчлэгдсэн хуудсууд:
- `audit_log_page.html` — CSV → xlsx, товч "Excel" болгосон
- `equipment_journal.html` — Monthly export → xlsx
- `equipment_eq_journal.html` — Journal export → xlsx
- `chemicals/waste_report.html` — Export → xlsx
- `chemicals/chemical_journal.html` — HTML table → AG-Grid + xlsx

### CSP асуудал шийдсэн:
- `cdnjs.cloudflare.com` блоклогдсон → SheetJS local download
- `cdn.datatables.net` блоклогдсон → `cdn.jsdelivr.net` солисон
- CSP whitelist: `cdn.jsdelivr.net`, `unpkg.com`, `'self'`

---

## DataTables нэмсэн - ДУУССАН ✅
- `base.html`-д DataTables CSS + JS (jsdelivr CDN)
- Quality dashboard-ууд (complaints, CAPA, improvements) DataTables ашигладаг
- `complaints_dashboard.html` — column widths тохируулсан, Excel export нэмсэн

---

## Chemical Journal AG-Grid - ДУУССАН ✅
- `chemicals/chemical_journal.html` — HTML table → AG-Grid
- `chemicals/crud.py` — `chemical_journal()` route JSON `rows_json` дамжуулдаг болсон
- Баганууд: Огноо (200px), Химийн бодис (flex:2), Тоо хэмжээ, Зорилго, Шинжилгээ, Хэрэглэгч, Нөөц

---

## Equipment Journal Improvements - ДУУССАН ✅

### Тусдаа багажийн журнал хуудас
- `equipment_eq_journal.html` — шинэ template
- `equipment/crud.py` — `equipment_journal_page(id)` route
- Filter pills: Бүгд | Ашиглалт | Шалгалт | Засвар | Их засвар | Калибровка
- GROUP_MAP: action_type → group mapping
- Backward compatibility: "Calorimeter Check" → "Calorimeter Calibration"

### Balance Ethanol Test
- `analysis_page.html` — ethanol pycnometer test panel нэмсэн
- `equipment/api.py` — ethanol handler, calorimeter calibration rename

---

## Mass Workspace Refactor - ДУУССАН ✅
- `mass_aggrid.html` — бүрэн дахин бичсэн
- `LIMS_AGGRID.createGrid()` factory ашиглаж:
  - `onCellKeyDown` — Arrow key navigation (editable columns)
  - `navigateToNextCell` — Tab/Enter navigation
  - `singleClickEdit`, `stopEditingWhenCellsLoseFocus`
- `LIMSDraftManager('mass')` нэмсэн:
  - `onCellValueChanged` → `saveDrafts()` (localStorage)
  - `setRowData()` → `restoreDrafts()` (refresh хийсэн ч утга алга болохгүй)
  - Save/Delete → `draftMgr.purge()` (хадгалагдсан draft устгах)
- Давхар checkbox устгасан (ID баганад нэгтгэсэн)
- ID `sort: 'asc'` нэмсэн

---

## Commit
- `e9f9a5e` — feat: Excel export, equipment journal, mass workspace improvements
