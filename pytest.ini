[pytest]
# ============================================================================
# Pytest Configuration for Coal LIMS
# ============================================================================

# Test discovery patterns
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Command line options (default)
addopts =
    # Verbose output
    --verbose
    --strict-markers

    # Coverage options
    --cov=app
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=25

    # Show extra test summary
    -ra

    # Show locals in tracebacks
    --showlocals

# Warning filters (library-ийн warnings-ийг нуух)
filterwarnings =
    # Flask-SQLAlchemy-ийн LegacyAPIWarning-ийг нуух
    ignore::sqlalchemy.exc.LegacyAPIWarning:flask_sqlalchemy.*
    # venv доторх бүх warnings нуух
    ignore::DeprecationWarning:.*site-packages.*

# Test markers (тодорхой тест group ажиллуулахад)
markers =
    smoke: Smoke tests (санамсаргүй ажиллуулна)
    unit: Unit tests
    integration: Integration tests
    security: Security tests
    slow: Slow tests (skip by default)
    database: Tests requiring database
    api: API endpoint tests

# Minimum Python version
minversion = 3.8

# Coverage options
[coverage:run]
source = app
omit =
    */tests/*
    */venv/*
    */migrations/*
    */__pycache__/*
    */site-packages/*

[coverage:report]
precision = 2
show_missing = True
skip_covered = False

exclude_lines =
    # Стандарт exclusions
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
    @abc.abstractmethod
